# Whisper.cpp Configuration

# Model size: tiny, tiny.en, base, base.en, small, small.en, medium, medium.en, large
# Larger models = better accuracy but slower and more RAM
WHISPER_MODEL=base

# Language hint (optional, helps with accuracy)
# Examples: en, es, fr, de, ja, zh, auto
WHISPER_LANGUAGE=auto

# Number of CPU threads (default: 4)
WHISPER_THREADS=4

# Port to expose the service on
WHISPER_PORT=9000
