# Test environment configuration
# Copy this to .env.local for local testing

# RPC endpoint for Base network - use a reliable RPC for testing
RPC_BASE_HTTP=https://mainnet.base.org

# Database connection (matches docker-compose.yml)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=amica_indexer
DB_USER=postgres
DB_PASS=postgres

# Construct DB_URL from components
DB_URL=postgres://postgres:postgres@localhost:5432/amica_indexer

# GraphQL server port
GQL_PORT=4000

# Processor options
PROCESSOR_PROMETHEUS_PORT=3000

# Optional: Use a local Base node or testnet
# RPC_BASE_HTTP=http://localhost:8545
